mixin partner(name, imgPath)
  - const path = `img/partners/${imgPath}`;

  li.partners__item
    a.partners__link(href="#!")
      .partners__img
        img(src=path, alt="partner logo")
      .partners__name
          span= name